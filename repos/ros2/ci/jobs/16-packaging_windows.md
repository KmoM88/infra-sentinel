# packaging_windows Job Analysis

## Overview

The `packaging_windows` job is a nightly-triggered Jenkins pipeline for building and packaging ROS 2 on Windows. It is dynamically generated by the `create_jenkins_job.py` script and uses the `packaging_job.xml.em` template. This job is the automated, nightly version of the manually-triggered `ci_packaging_windows` job.

## Job Generation

The `create_jenkins_job.py` script generates the `packaging_windows` job for the `windows` OS. It's designed to create Chocolatey packages for the ROS 2 packages in the workspace on a nightly basis.

## Windows Packaging-Specific Configuration

The `packaging_windows` job has the following key configuration settings:

*   **Job Name:** The job is explicitly named `packaging_windows`.
*   **Template:** It uses the `packaging_job.xml.em` template.
*   **Jenkins Node Label:** It is configured to run on a Jenkins node with the `windows-container` label.
*   **Build Type:** The `cmake_build_type` is set to `RelWithDebInfo`.
*   **Trigger:** The job is triggered by a timer (`time_trigger_spec`) set to run daily at 4:00 AM (`0 4 * * *`).
*   **Notifications:** Email notifications for build results are sent to `ros2-buildfarm@googlegroups.com`.
*   **Packaging Flag:** The `--packaging` argument is passed to the `run_ros2_batch.py` script, which triggers the packaging logic.
*   **Docker Image:** The Docker image is tagged as `ros2_windows_ci_<ROS_DISTRO>`.

## Execution Flow

The execution flow of the `packaging_windows` job is identical to the `ci_packaging_windows` job.

1.  **SCM Checkout:** The job checks out the `ros2/ci` repository.

2.  **Docker Environment:** The job runs inside a Docker container.

    *   **Dockerfile Selection:** The job uses the `windows_docker_resources/Dockerfile`.
    *   **Docker Image Build:** A new Docker image is built from `Dockerfile` and tagged as `ros2_windows_ci_<ROS_DISTRO>`.
    *   **Docker Container Run:** The job runs the newly built Docker image, passing the specific build and test arguments, including the `--packaging` flag, via the `CI_ARGS` environment variable.

3.  **`run_ros2_batch.py`:** The `ros2_batch_job` Python module within the container handles the build process. When the `--packaging` flag is present, it generates Chocolatey packages for all the ROS 2 packages in the workspace.

## Configuration and Scripts

*   **Job Generation Script:**
    *   `create_jenkins_job.py`: The script that generates the `packaging_windows` job.
*   **Jenkins Job Template:**
    *   `job_templates/packaging_job.xml.em`: The template for the packaging job.
*   **Build Logic:**
    *   `run_ros2_batch.py`: The entry point for the build process inside the Docker container.
*   **Docker configuration:**
    *   `windows_docker_resources/Dockerfile`: The Dockerfile for Windows builds.

## Parameters

The `packaging_windows` job accepts the same parameters as the `ci_packaging_windows` job, but it is typically run with the default values since it is triggered automatically.
